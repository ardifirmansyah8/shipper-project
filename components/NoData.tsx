import { FC } from "react";

const NoData: FC = () => (
  <div className="flex h-56 items-center justify-center border border-gray-300 text-lg font-bold">
    No Data
  </div>
);

export default NoData;
